node_modules/
dist/
*.tsbuildinfo

# Generated by the TanStack Router plugin / `tsr generate`
apps/web/src/routeTree.gen.ts

# Postgres data (bind mount from docker-compose.yml)
.docker-data/

# Built SPA, written by `pnpm build` into the server's static directory
apps/server/public/

# Uploaded templates, generated invoice PDFs, note attachments.
# Never web-root, never in version control.
apps/server/data/*
!apps/server/data/.gitkeep

.env
.env.local
.DS_Store

# /design-sync scratch — regenerated, never committed
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules
apps/web/.ds-sync-scratch/
